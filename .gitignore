# ----------------------------------------------------------------------
# Meson Build System / Compilation Outputs
# ----------------------------------------------------------------------
# Meson's default build directory, where all compilation outputs go.
build/
# Meson's default unit test log directory (e.g., if you run 'meson test')
meson-logs/
# Meson's private file cache used for configuration.
.meson_lock

# ----------------------------------------------------------------------
# Meson Wraps (Subprojects)
# ----------------------------------------------------------------------
# Meson-managed dependencies fetched from WrapDB.
# These should NOT be committed, as Meson fetches them automatically.
subprojects/

# ----------------------------------------------------------------------
# External Dependencies (Fully managed by install_dependencies.bat)
# ----------------------------------------------------------------------
# This folder contains vcpkg and all its built/downloaded files.
# It is completely ignored and must be regenerated by the setup script.
external/

# ----------------------------------------------------------------------
# General C/C++ Compilation Artifacts (Safety Net)
# ----------------------------------------------------------------------
*.o
*.obj
*.exe
*.dll
*.lib
*.so
*.a
*.pdb  # Debug symbols
*.ilk

# ----------------------------------------------------------------------
# IDE & OS Files
# ----------------------------------------------------------------------
# VS Code (User-specific settings, if not shared)
#.vscode/settings.json --- this is shared
.vscode/*.log
.vscode/*.cache

# Windows Thumbnail and system files
Thumbs.db
Desktop.ini